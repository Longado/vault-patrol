Vault patrol is an event-driven agent. A push to the vault hits a Cloud Run service. Code does the mechanical checks: broken links, orphans, dangling wiki links. Then Gemini three point five Flash gets exactly one structured call to judge five kinds of semantic rot. Then code verifies every quote the model returned against the real file. Whatever survives becomes a single pull request. Subtraction only.
